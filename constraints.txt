torch==0  # Not needed as it is commented out in your app.py
Pillow==0  # Not needed unless you use code related to image processing
transformers==0  # Not directly required
scipy==0  # Not directly required
